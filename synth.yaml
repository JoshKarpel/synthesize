targets:
  - id: tests
    commands: pytest
    lifecycle:
      type: "watch"
      paths: ["synth", "tests"]
  - id: types
    commands: mypy
    lifecycle:
      type: "watch"
      paths: ["synth", "tests"]
  - id: docs
    commands: mkdocs serve --strict
    lifecycle:
      type: "restart"
